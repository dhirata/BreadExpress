<h3 class="text-center">Items for Sale</h3>

<div class="row">
  <div class="small-6 column small-offset-3 columns">
    <% @items.each do |item| %>
      <fieldset>
      <%= render "item_row", item: item, order_item: @order_item %>
      </fieldset>
    <% end %>
  </div>
</div>