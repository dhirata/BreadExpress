<%= link_to "#{current_order.order_items.size} Items in Cart ( #{number_to_currency current_order.order_items.collect { |oi| oi.valid? ? (oi.quantity * oi.item.current_price) : 0 }.sum} )", cart_path, class: "btn btn-link" %>